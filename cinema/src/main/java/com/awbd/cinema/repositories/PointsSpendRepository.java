package com.awbd.cinema.repositories;

import com.awbd.cinema.entities.PointsSpend;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PointsSpendRepository extends JpaRepository<PointsSpend, Long> {
}
